<% flash.each do |name, msg| %>
  <div id="flash_alert", data-alert data-options="animation_speed:1500, animation:'fadeOut';" class="alert-box <%= flash[:notice] ? "success" : "warning" %> radius">
    <a href="#" class="close">&times;</a>
    <%= msg %>
  </div>
<% end %>