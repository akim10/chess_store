<div id="mySidenav" class="sidenav">
  <a href="javascript:void(0)" class="closebtn" onclick="closeNav()">&times;</a>
  <% if @cart_items.nil? || @cart_items.empty?%>
    <h4> Your Cart is Empty! </h4>
  <% else %>
    <%= render :partial => "partials/cart_items" %>
  <% end %>
</div>